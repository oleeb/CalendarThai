<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".CalendarThai">
    <item android:id="@+id/menu_action_settings"
        android:title="@string/txt_settings"
        android:icon="@drawable/ic_action_settings"
        android:showAsAction="always"
        tools:ignore="AppCompatResource" />
    <item android:id="@+id/menu_action_refresh"
        android:title="@string/txt_refresh"
        android:icon="@drawable/ic_action_refresh"
        android:showAsAction="always"
        tools:ignore="AppCompatResource" />
</menu>
